import React from "react";
import ReactDOM from "react-dom/client";
import RoomDesignerApp from "./RoomDesignerApp";
import "./index.css"; // 스타일 시트 (선택사항)

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RoomDesignerApp />
  </React.StrictMode>
);
